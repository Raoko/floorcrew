# FBS-DSR Cable Pull Impact Visualizer — Tasks

## Pending

- [ ] Create devices.json with schema: id, hostname, role (FBS/DSR), rack, zone, row, port_count
- [ ] Populate devices.json with at least 10 FBS switches (FBS01–FBS10) and 4 DSR switches
- [ ] Create cables.json with schema: cable_id, from_device, from_port, to_device, to_port, redundancy_group, status
- [ ] Populate cables.json with 30+ cable entries covering multiple redundancy groups
- [ ] Ensure each FBS has at least 2 uplinks in the same redundancy_group for demo scenarios
- [ ] Add a single-uplink FBS entry to cables.json for HIGH risk demo scenario
- [ ] Create impact.py with lookup_cable(cable_id) function that returns cable metadata
- [ ] Create impact.py find_redundancy_group(cable_id) function returning all cables in the same group
- [ ] Create impact.py count_healthy_uplinks(redundancy_group, already_down=[]) function
- [ ] Create impact.py compute_risk(healthy_before, healthy_after) returning LOW/MEDIUM/HIGH + reason
- [ ] Implement risk table: HIGH if single-uplink; HIGH if last uplink; MEDIUM if 2->1; LOW if 3+->2+
- [ ] Create impact.py generate_maint_snippet(cable, risk, remaining) returning paste-ready MAINT text
- [ ] Create impact.py analyze(cable_id, already_down=[]) returning full impact dict
- [ ] Add safety disclaimer text to all outputs: "Planning tool using static topology..."
- [ ] Create cli.py with argparse: positional cable_id, optional --down flags for pre-marked cables
- [ ] CLI prints risk card: cable info, from/to, zone, rack, risk level, remaining uplinks, suggested action
- [ ] CLI prints MAINT snippet after risk card (clearly delimited)
- [ ] CLI supports --json flag to output machine-readable JSON
- [ ] CLI supports --list flag to show all available cable IDs
- [ ] CLI colorizes output: HIGH=red, MEDIUM=yellow, LOW=green (using ANSI codes)
- [ ] CLI handles unknown cable_id gracefully with helpful error message
- [ ] Test CLI demo scenario 1: both uplinks healthy, pull one -> MEDIUM output
- [ ] Test CLI demo scenario 2: one already down (--down), pull other -> HIGH output
- [ ] Test CLI demo scenario 3: single-uplink FBS -> HIGH output immediately
- [ ] Create app.py Flask app with route GET / serving index.html
- [ ] Create app.py route GET /api/cables returning all cable IDs and metadata
- [ ] Create app.py route POST /api/analyze with body {cable_id, already_down:[]} returning impact JSON
- [ ] Create app.py route GET /api/redundancy-groups returning all groups with member counts
- [ ] Flask app loads devices.json and cables.json once at startup (not on every request)
- [ ] Flask app returns proper JSON errors with 400/404 status codes for bad input
- [ ] Create templates/ directory
- [ ] Create templates/index.html as single-page app (no external CDN deps, inline CSS+JS)
- [ ] index.html has cable picker: searchable dropdown of all cable IDs
- [ ] index.html has "Mark as already down" checkboxes for cables in the same redundancy group
- [ ] index.html has "Check Impact" button that calls POST /api/analyze
- [ ] index.html renders risk card: color-coded banner (red/yellow/green) with risk level
- [ ] index.html shows: cable ID, from device/port, to device/port, zone, rack
- [ ] index.html shows: uplinks before pull, uplinks after pull, risk level, reason
- [ ] index.html shows: suggested action text based on risk level
- [ ] index.html shows: paste-ready MAINT snippet in a copyable code block
- [ ] index.html has "Copy MAINT" button that copies snippet to clipboard
- [ ] index.html shows safety disclaimer banner at top of page (always visible)
- [ ] index.html resets "already down" checkboxes when a new cable is selected
- [ ] index.html handles API errors gracefully (show error banner, not broken UI)
- [ ] Style index.html: dark theme appropriate for DCT terminal environment
- [ ] Style risk card with clear color coding (no ambiguity about severity)
- [ ] Add favicon to Flask app (simple colored circle matching risk level or site logo)
- [ ] Create requirements.txt: flask>=2.0, python-dotenv (optional)
- [ ] Create run.sh: activates venv if present, sets FLASK_ENV=development, runs app.py
- [ ] Create test_impact.py with unit tests for compute_risk() function
- [ ] Test compute_risk: 1 healthy before pull (single) -> HIGH
- [ ] Test compute_risk: 2 healthy, 1 already_down, pull active -> HIGH
- [ ] Test compute_risk: 2 healthy, pull one -> MEDIUM
- [ ] Test compute_risk: 3 healthy, pull one -> LOW
- [ ] Test compute_risk: 4 healthy, pull one -> LOW
- [ ] Test analyze() returns expected structure for each scenario
- [ ] Test generate_maint_snippet() includes cable_id, device, port, risk level
- [ ] Test lookup_cable() raises ValueError for unknown cable_id
- [ ] Test find_redundancy_group() returns all cables with same redundancy_group value
- [ ] Test count_healthy_uplinks() excludes already_down cables from count
- [ ] Create README.md: project overview, how to run CLI, how to run web UI
- [ ] README includes demo scenarios as step-by-step instructions
- [ ] README includes sample output screenshots (ASCII art if no images)
- [ ] Create demo.sh: runs 3 demo scenarios in sequence with echo descriptions
- [ ] Create demo scenario output files: scenario1.txt, scenario2.txt, scenario3.txt
- [ ] Create slides/ directory with 4 slide content files (md format)
- [ ] slides/01-problem.md: "Today this is tribal knowledge" — who/when/how often
- [ ] slides/02-solution.md: what the tool does, key features
- [ ] slides/03-demo.md: demo scenario walk-through notes
- [ ] slides/04-future.md: what comes next (live NetBox integration, Slack bot, etc.)
- [ ] Add --scenario flag to CLI for running pre-canned demo scenarios by number
- [ ] Add devices lookup to risk card output (show full rack info for affected device)
- [ ] Validate cables.json on startup: every cable's devices must exist in devices.json
- [ ] Validate cables.json on startup: every redundancy_group has at least 2 cables
- [ ] Add verbose mode (-v) to CLI showing intermediate computation steps
- [ ] Add app.py route GET /api/devices for device metadata
- [ ] Add app.py route GET /api/health for healthcheck
- [ ] index.html shows device detail panel when cable is selected (zone, rack, role)
- [ ] Add zone filter to cable picker in index.html (show only cables in selected zone)
- [ ] Add "Scenario Mode" toggle in UI that enables multiple "already down" selections
- [ ] Add keyboard shortcut in UI: Enter submits analysis, Escape clears selection
- [ ] Test Flask routes return correct HTTP status codes for all error cases
- [ ] Test POST /api/analyze with empty body returns 400
- [ ] Test POST /api/analyze with unknown cable_id returns 404
- [ ] Add CORS headers to Flask app for local dev (allow localhost origins)
- [ ] Create .gitignore: __pycache__, *.pyc, .env, venv/, *.egg-info
- [ ] Initialize git repo in fbs-dsr-cable-impact/ with initial commit
- [ ] Write opening demo script: "Imagine we're about to reseat Vinakom port c33 on DSR1..."
- [ ] Create video_notes.md: shot-by-shot script for the 2-3 min demo recording
- [ ] Ensure MAINT snippet format matches real CoreWeave MAINT ticket format
- [ ] Add impact summary line: "Pulling FBS01_UP_A: EVI01-ZONE-A will drop from 2 to 1 uplinks. Risk: MEDIUM."
- [ ] Verify all 3 demo scenarios produce distinct, clear outputs before demo recording
- [ ] Add timestamp to MAINT snippet (current date/time formatted for ticket)
- [ ] Add --dry-run flag: shows what would be output without marking anything down
- [ ] Create data_model.md documenting devices.json and cables.json schemas
- [ ] Update CLAUDE.md status checkboxes as tasks are completed
- [ ] Create hackathon pitch one-liner comment in app.py header
- [ ] Final review: ensure safety disclaimer appears in every output path (CLI and web)
- [ ] Test full demo flow end-to-end: open app, select cable, mark one down, check HIGH risk
- [ ] Ensure index.html works without internet (all assets inline, no CDN calls)
- [ ] Create install.sh: creates venv, installs requirements, prints run instructions

## Done

## Failed
